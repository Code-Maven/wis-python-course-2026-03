* Read the source code from the files in Day 1
* Add command line version
* Add GUI
* Convert the circle and rectangle examples into functions and separate the I/O from the business logic (the computation).
* move the functions to a module.


* Assignment: Come up with some interesting computation.


* Add command line interface (argv)
* Add tests

