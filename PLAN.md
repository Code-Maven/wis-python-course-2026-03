* Algorithm and syntax

Exercise:
* Write a function that will convert Roman numbers to Arabic numbers (integers).
  1. Enough to handle between 1-3999.
  1. Try to handle more numbers.
  1. Write tests.
  1. Raise an exception if an invalid string is encountered.

* Transpose text. Write a function that receives a string and a number `transpose(text, width)`
  input: "abcdefg", 2
  intermediate:

   ab
   cd
   ef
   g

   result:
   acegbdf

  input: "abcdefg", 2
  intermediate:
  abc
  def
  g
  result:
  adgbecf


* Read the source code from the files in Day 2
* Go over data types in python.
* Read Excel and other files.
* `requirements.txt` and `pip install`
* uv

